/**
 * Calculates the average of a list of numbers.
 *
 * @param {numbers[]} [nums=[]] - The numbers to average.
 * @returns {number} The average of all numbers.
 */
export const average = (nums = []) => {};
