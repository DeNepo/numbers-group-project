import { listItems } from './list-items.js';

// hint: inspect the expected output in the web page's DOM
describe('listItems: converts and array into a string list with `*` bullet points', () => {});
